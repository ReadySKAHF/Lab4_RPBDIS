<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - AutoRepairWorkshop</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- FontAwesome для иконок -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet" />

    <style>
        /* Общие стили для всех страниц */
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        /* Стили для шапки */
        header {
            background-color: #343a40;
            color: #fff;
            padding: 15px 0;
            text-align: center;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

            header nav ul {
                list-style: none;
                padding: 0;
                margin: 0;
                display: flex;
                justify-content: center;
                flex-wrap: wrap;
            }

                header nav ul li {
                    margin: 0 15px;
                }

                    header nav ul li a {
                        color: #fff;
                        text-decoration: none;
                        font-weight: bold;
                        padding: 10px 20px;
                        transition: background-color 0.3s ease;
                    }

                        header nav ul li a:hover {
                            background-color: #495057;
                            border-radius: 5px;
                        }

        /* Стили для контейнера */
        .container {
            padding: 20px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            border-radius: 8px;
            margin-top: 20px;
        }

        h1 {
            font-weight: bold;
            margin-bottom: 20px;
            color: #333;
        }

        /* Стили для таблиц */
        .table {
            border-collapse: collapse;
            width: 100%;
        }

            .table th, .table td {
                padding: 12px 15px;
                text-align: left;
                border: 1px solid #ddd;
            }

            .table th {
                background-color: #343a40;
                color: #fff;
            }

        .table-striped tbody tr:nth-of-type(odd) {
            background-color: #f2f2f2;
        }

        .table-bordered {
            border: 1px solid #ddd;
        }

        /* Стили для кнопок действий */
        .btn-edit {
            color: #fff;
            background-color: #007bff; /* Синий цвет */
            border: none;
            padding: 5px 10px;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

            .btn-edit:hover {
                background-color: #0056b3;
                text-decoration: none;
            }

        .btn-details {
            color: #fff;
            background-color: #28a745; /* Зеленый цвет */
            border: none;
            padding: 5px 10px;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

            .btn-details:hover {
                background-color: #1e7e34;
                text-decoration: none;
            }

        .btn-delete {
            color: #fff;
            background-color: #dc3545; /* Красный цвет */
            border: none;
            padding: 5px 10px;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }

            .btn-delete:hover {
                background-color: #bd2130;
                text-decoration: none;
            }

        .btn-create {
            color: #fff;
            background-color: #6f42c1; /* Фиолетовый цвет */
            border: none;
            padding: 8px 15px;
            border-radius: 4px;
            font-weight: bold;
            display: inline-flex;
            align-items: center;
            transition: background-color 0.3s ease;
        }

            .btn-create:hover {
                background-color: #5a32a3;
                text-decoration: none;
            }

            .btn-create i {
                margin-right: 5px;
            }

        /* Стили для футера */
        footer {
            text-align: center;
            padding: 20px 0;
            background-color: #343a40;
            color: #fff;
            margin-top: 20px;
        }

            footer p {
                margin: 0;
            }
    </style>
</head>
<body>
    <header>
        <nav>
            <ul class="d-flex flex-wrap justify-content-center">
                <li><a asp-controller="Cars" asp-action="Index">Cars</a></li>
                <li><a asp-controller="CarServices" asp-action="Index">CarServices</a></li>
                <li><a asp-controller="CarStatus" asp-action="Index">CarStatus</a></li>
                <li><a asp-controller="Mechanics" asp-action="Index">Mechanics</a></li>
                <li><a asp-controller="Owners" asp-action="Index">Owners</a></li>
                <li><a asp-controller="Payments" asp-action="Index">Payments</a></li>
                <li><a asp-controller="RepairOrders" asp-action="Index">RepairOrders</a></li>
            </ul>
        </nav>
    </header>

    <div class="container">
        @RenderBody()
    </div>

    <footer>
        <p>&copy; @DateTime.Now.Year - AutoRepairWorkshop. All rights reserved.</p>
    </footer>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
